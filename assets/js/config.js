const config = {
    // Change this to your deployed backend URL (e.g., https://fitcalc-api.onrender.com)
    // Keep as http://localhost:8000 for local development
    API_BASE_URL: "http://localhost:8000"
};
